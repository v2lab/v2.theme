<?xml version="1.0"?>

<portlets>
	<!-- Reassigning the navigation portlet from the left bar -->
	<assignment
		manager="plone.leftcolumn"
		category="context"
		key="/"
		type="portlets.Classic"
		name="navigation"
	/>
	
	<!-- Reassigning the login portlet from the left bar -->
        <assignment
                manager="plone.leftcolumn"
                category="context"
                key="/"
                type="portlets.Login"
                name="login"
        />

</portlets>
